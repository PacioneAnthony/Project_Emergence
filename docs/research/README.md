# Historical research branch

These documents preserve the simulation, JEPA and LNN experiments that informed decision D-002. They are evidence and reproducibility material, not the active project roadmap.

Executable runners live in `scripts/research/`; reusable modules remain in `common/`, `sim2d/` and `learning/`.

Current experimental continuation: `developmental_curiosity_probe.md` documents the
continuous curiosity scheduler introduced after the regional learning-progress probe was
rejected. It remains gated by D-002 and is not the active physical roadmap.
